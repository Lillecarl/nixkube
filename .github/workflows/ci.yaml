# SPDX-License-Identifier: MIT
# GENERATED FILE -- do not edit by hand.
# Edit ci/workflows/ci.nix, then run: nix run --file . ci-workflow-update
concurrency:
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
  group: ${{ github.workflow }}-${{ github.ref }}
env:
  CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
  REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  REPO_USERNAME: ${{ github.actor }}
  UMBRELLA_GIT: '1'
jobs:
  build-amd64:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Build and push environments
        run: |
          unlink nix-envs || true
          nix build --show-trace --print-build-logs --file . push --out-link nix-envs
          ./nix-envs/bin/push
      - name: Build amd64 nix image
        run: |
          unlink nix-nix || true
          nix build --show-trace --print-build-logs --file . nixImage.pushArch.x86_64-linux --out-link nix-nix
      - if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')
        name: Push amd64 nix image
        run: ./nix-nix/bin/push-nix-x86_64-linux
    timeout-minutes: 45
  build-arm64:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - umbrella-rev
    runs-on: ubuntu-24.04-arm
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Build and push environments
        run: |
          unlink nix-envs || true
          nix build --show-trace --print-build-logs --max-jobs 1 --cores 4 --file . push --out-link nix-envs
          ./nix-envs/bin/push
      - name: Build arm64 nix image
        run: |
          unlink nix-nix || true
          nix build --show-trace --print-build-logs --max-jobs 1 --cores 4 --file . nixImage.pushArch.aarch64-linux --out-link nix-nix
      - if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')
        name: Push arm64 nix image
        run: ./nix-nix/bin/push-nix-aarch64-linux
    timeout-minutes: 90
  build-manifests:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - build-amd64
      - build-arm64
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Assert both architectures' closures are fetchable
        run: |
          nix build --show-trace --file . kubenixApply.manifestJSONFile --out-link manifest
          nix run --file . assert-cached -- manifest
      - name: Build the index and scratch publishers
        run: |
          unlink nix-manifest || true
          unlink nix-scratches || true
          nix build --show-trace --file . nixImage.pushManifest --out-link nix-manifest
          nix build --show-trace --file . scratchImage.push --out-link nix-scratches
      - if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')
        name: Create nix multi-arch manifest
        run: ./nix-manifest/bin/push-nix-manifest
      - if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')
        name: Push scratch image
        run: ./nix-scratches/bin/push
      - if: env.CACHIX_AUTH_TOKEN != ''
        name: Push nocache variant store paths
        run: nix run --file . push-ci2
      - if: env.CACHIX_AUTH_TOKEN != ''
        name: Push test workload store paths
        run: nix run --file . push-citest
    timeout-minutes: 30
  check:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Run type checker
        run: nix develop --file shell.nix --command pyright pkgs/nixkube/src
      - name: Run the checks
        run: nix build --show-trace --file ./checks.nix all
      - name: Check that every CI attribute builds natively
        run: |
          nix run --file . arch-audit -- \
            push \
            push-ci2 \
            push-citest \
            kubenixApply.manifestJSONFile \
            kubenixApply.manifestYAMLFile \
            kubenixCI1.deploymentScript \
            kubenixCI2.deploymentScript \
            kubenixCITest.deploymentScript \
            nixImage.pushArch.x86_64-linux \
            nixImage.pushManifest \
            scratchImage.push
      - name: Check formatting
        run: |
          nix run --file . treefmt
          git diff --exit-code
    timeout-minutes: 30
  docs-build:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    if: github.ref == 'refs/heads/develop'
    needs:
      - umbrella-rev
    runs-on: ubuntu-24.04
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - timeout-minutes: 15
        uses: cachix/cachix-action@master
        with:
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          name: nixkube
          useDaemon: false
      - name: Build documentation
        run: nix build --file . nixkube-docs --out-link result --print-build-logs --print-out-paths
      - name: Verify docs closure
        run: nix store verify --recursive --no-trust "$(readlink -f result)"
        timeout-minutes: 15
      - name: Prepare the Pages artifact
        run: mkdir -p public; cp -r --no-preserve=mode,ownership result/. public/
        timeout-minutes: 5
      - timeout-minutes: 10
        uses: actions/upload-pages-artifact@main
        with:
          path: public
    timeout-minutes: 30
  docs-deploy:
    concurrency:
      cancel-in-progress: false
      group: pages
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    if: github.ref == 'refs/heads/develop'
    needs:
      - docs-build
      - umbrella-rev
    permissions:
      id-token: write
      pages: write
    runs-on: ubuntu-24.04
    steps:
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - id: deployment
        name: Deploy to GitHub Pages
        timeout-minutes: 10
        uses: actions/deploy-pages@main
    timeout-minutes: 15
  release:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - check
      - build-manifests
      - test-kind-cache
      - test-kind-nocache
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Render deployment manifest
        run: |
          nix build --show-trace --file . kubenixApply.manifestYAMLFile
          cp result nix-csi-deployment.yaml
      - name: Assert the release manifest is deployable
        run: nix run --file . assert-cached -- nix-csi-deployment.yaml
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          files: nix-csi-deployment.yaml
          prerelease: false
    timeout-minutes: 30
  test-kind-cache:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - build-manifests
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Create Kind cluster
        uses: helm/kind-action@main
      - name: Clean runner
        run: |
          # Remove cache because permissions can get fucked up preventing kluctl from creating it's cache directory
          sudo rm --recursive --force /home/runner/.cache
      - name: Deploy nix-csi
        run: |
          nix build --show-trace --file . kubenixCI1.deploymentScript
          ./result/bin/kubenixDeploy --yes
      - name: Wait for nix-csi node daemonset
        run: |
          kubectl rollout status daemonset -l app.kubernetes.io/component=node -n nixkube --timeout=180s
      - name: Wait for nix-csi cache pod
        run: |
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/component=pynixd -n nixkube --timeout=180s
      - name: Deploy test workloads
        run: |
          nix build --show-trace --file . kubenixCITest.deploymentScript
          ./result/bin/kubenixDeploy --yes
      - name: Wait for test workload
        run: |
          kubectl wait --for=condition=complete job/flake-hello job/expr-hello job/path-hello job/commandpath-hello job/env-ssl job/nri-hello-ro job/nri-hello-rw -n nixkube --timeout=300s
      - name: Check the invalid workloads were refused
        run: |
          for job in invalid-storepath-hello invalid-flake-hello invalid-expr-hello; do
            ok=$(kubectl get job "$job" -n nixkube -o jsonpath='{.status.succeeded}')
            if [ -n "$ok" ] && [ "$ok" != 0 ]; then
              echo "$job succeeded, and it asks for something that cannot be built" >&2
              exit 1
            fi
          done
      - name: Delete jobs and verify CSI cleanup
        run: |
          kubectl delete job flake-hello expr-hello path-hello commandpath-hello env-ssl nri-hello-ro nri-hello-rw invalid-storepath-hello invalid-flake-hello invalid-expr-hello -n nixkube
          kubectl wait --for=delete pod -l "job-name in (flake-hello,expr-hello,path-hello,commandpath-hello,env-ssl,nri-hello-ro,nri-hello-rw,invalid-storepath-hello,invalid-flake-hello,invalid-expr-hello)" -n nixkube --timeout=120s
      - env:
          DS_API: ${{ secrets.DS_API }}
        if: failure()
        name: Debug on failure
        run: nix run --file . ci-debug || true
    timeout-minutes: 45
  test-kind-nocache:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - build-manifests
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Create Kind cluster
        uses: helm/kind-action@main
      - name: Clean runner
        run: |
          sudo rm --recursive --force /home/runner/.cache
      - name: Deploy nix-csi
        run: |
          nix build --show-trace --file . kubenixCI2.deploymentScript
          ./result/bin/kubenixDeploy --yes
      - name: Wait for nix-csi node daemonset
        run: |
          kubectl rollout status daemonset -l app.kubernetes.io/component=node -n nixkube --timeout=180s
      - name: Deploy test workloads
        run: |
          nix build --show-trace --file . kubenixCITest.deploymentScript
          ./result/bin/kubenixDeploy --yes
      - name: Wait for test workload
        run: |
          kubectl wait --for=condition=complete job/flake-hello job/expr-hello job/path-hello job/commandpath-hello job/env-ssl job/nri-hello-ro job/nri-hello-rw -n nixkube --timeout=300s
      - name: Check the invalid workloads were refused
        run: |
          for job in invalid-storepath-hello invalid-flake-hello invalid-expr-hello; do
            ok=$(kubectl get job "$job" -n nixkube -o jsonpath='{.status.succeeded}')
            if [ -n "$ok" ] && [ "$ok" != 0 ]; then
              echo "$job succeeded, and it asks for something that cannot be built" >&2
              exit 1
            fi
          done
      - name: Delete jobs and verify CSI cleanup
        run: |
          kubectl delete job flake-hello expr-hello path-hello commandpath-hello env-ssl nri-hello-ro nri-hello-rw invalid-storepath-hello invalid-flake-hello invalid-expr-hello -n nixkube
          kubectl wait --for=delete pod -l "job-name in (flake-hello,expr-hello,path-hello,commandpath-hello,env-ssl,nri-hello-ro,nri-hello-rw,invalid-storepath-hello,invalid-flake-hello,invalid-expr-hello)" -n nixkube --timeout=120s
      - env:
          DS_API: ${{ secrets.DS_API }}
        if: failure()
        name: Debug on failure
        run: nix run --file . ci-debug || true
    timeout-minutes: 45
  test-qemu:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Allow the unprivileged user namespaces Nix and passt need
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0; sudo sysctl -w kernel.unprivileged_userns_clone=1; unshare --user --map-root-user --mount --pid --fork --mount-proc true
        timeout-minutes: 5
      - name: Let the runner user open /dev/kvm
        run: echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules; sudo udevadm control --reload-rules; sudo udevadm trigger --name-match=kvm; ls -l /dev/kvm
        timeout-minutes: 5
      - name: Run the node test as a virtual machine
        run: nix run --file . umlTest.run -- --out ./uml-out
    timeout-minutes: 60
  test-qemu-ci:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - build-manifests
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Allow the unprivileged user namespaces Nix and passt need
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0; sudo sysctl -w kernel.unprivileged_userns_clone=1; unshare --user --map-root-user --mount --pid --fork --mount-proc true
        timeout-minutes: 5
      - name: Let the runner user open /dev/kvm
        run: echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules; sudo udevadm control --reload-rules; sudo udevadm trigger --name-match=kvm; ls -l /dev/kvm
        timeout-minutes: 5
      - name: Deploy and test without the pynixd cache, on a guest
        run: nix run --file . ciTest.run
    timeout-minutes: 60
  test-qemu-ci-cache:
    env:
      UMBRELLA_REV: ${{ needs.umbrella-rev.outputs.rev }}
    needs:
      - build-manifests
      - umbrella-rev
    runs-on: ubuntu-latest
    steps:
      - timeout-minutes: 10
        uses: actions/checkout@main
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - timeout-minutes: 15
        uses: cachix/install-nix-action@master
        with:
          extra_nix_config: |
            access-tokens = github.com=${{ github.token }}
            experimental-features = nix-command flakes
            substituters = https://cache.nixos.org?priority=1 https://nixkube.cachix.org?priority=2 https://lillecarl.cachix.org?priority=3
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nixkube.cachix.org-1:H8UE0jlI9pxHexK/NhDmEoLDarJXp1WTymQrsajlh7M= lillecarl.cachix.org-1:NN/LLMg7mbyvZCu32Qlo8LpSHqNw7Rr3VBCEYQvRpT0=
            trusted-users = root runner
      - name: Allow the unprivileged user namespaces Nix and passt need
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0; sudo sysctl -w kernel.unprivileged_userns_clone=1; unshare --user --map-root-user --mount --pid --fork --mount-proc true
        timeout-minutes: 5
      - name: Let the runner user open /dev/kvm
        run: echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules; sudo udevadm control --reload-rules; sudo udevadm trigger --name-match=kvm; ls -l /dev/kvm
        timeout-minutes: 5
      - name: Deploy and test with the pynixd cache, on a guest
        run: nix run --file . ciTestCache.run
    timeout-minutes: 60
  umbrella-rev:
    outputs:
      rev: ${{ steps.resolve.outputs.rev }}
    runs-on: ubuntu-24.04
    steps:
      - if: runner.os == 'Linux'
        name: Make room on the runner
        run: df -h /; sudo rm -rf /usr/lib/jvm /usr/share/dotnet /usr/share/swift /usr/local/.ghcup /usr/local/julia* /usr/local/lib/android /opt/az /usr/local/share/powershell /usr/local/share/chromium /opt/microsoft /opt/google /usr/lib/firefox; docker system prune --all --force; docker builder prune --all --force; df -h /
        timeout-minutes: 10
      - uses: actions/checkout@v4
        with:
          fetch-depth: 100
      - id: resolve
        name: Resolve the umbrella revision
        run: ci/walkback.sh https://github.com/nixidae/nixidae nixkube | sed 's/^/rev=/' >> "$GITHUB_OUTPUT"
    timeout-minutes: 5
'on':
  pull_request: null
  push:
    branches:
      - '*'
      - '!cidev'
    tags:
      - v*
  workflow_dispatch: null
permissions:
  actions: write
  contents: write
  packages: write
