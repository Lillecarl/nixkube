[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nri"
version = "0.1.0"
description = "NRI (Node Resource Interface) spec for Python"
authors = [
    { name="Carl Andersson", email="github@lillecarl.com" }
]
readme = "README.md"
requires-python = ">=3.12"
# What protoc writes imports: `nri_grpc.py` imports grpclib, `nri_pb2.py`
# imports google.protobuf.
dependencies = ["grpclib", "protobuf"]
